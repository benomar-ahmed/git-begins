La commande pour cloner un repository :

git clone [URL]

Les 3 commandes dans l'ordre pour psuh :

git add [Nom_du_fichier]
git commit –m “Description du commit”
git push origin master

La commande qui permet de pull :

git pull

La commande qui permet de créer une branche :

git branch [Nom_du_fichier]

La commande qui permet de switcher d'une branche à l'autre :

git checkout [Nom_de_la_branche]
